// viewDate = ({ loading, error, data }) => {//import { Query } from "@apollo/client/react/components";
//   if (loading) return null;
//   if (error) return console.log(error);
//   if (data.currencies === undefined) return null;
//   return data.currencies.map((item, index) => (
//     <div key={index}>{`${item.symbol} & ${item.label}`}</div>
//   ));
// };
/* <Query query={CURRENCY_DETIALS}>{this.viewDate}</Query> */

/*
    constructor(props) {
    super(props);

    this.state = {
      label: "",
      symbol: "",
    };
  }

  import client from "../Data";
  import { CURRENCY_DETIALS } from "../Data/data";
  componentDidMount() {
    client
      .query({
        query: CURRENCY_DETIALS,
      })
      .then((result) =>
        this.setState({
          label: result.data.currencies.map((item) => item.label),
          symbol: result.data.currencies.map((item) => item.symbol),
        })
      );
  } 
          {console.log(this.state.label)}
        <p>{this.state.label[3]}</p>
        <p>{this.state.symbol[1]}</p>*/

/* componentDidMount() {
    client
      .query({
        query: GET_PRODUCTS_BY_ID,
        variables: {
          id: "huarache-x-stussy-le",
        },
      })
      .then(
        (result) => console.log(result.data)
        // this.setState({
        //   label: result.data.currencies.map((item) => item.label),
        //   symbol: result.data.currencies.map((item) => item.symbol),
        // })
      );
  }*/

//  select

// import Select from "react-select";

// const options = [
//   { value: "dollar usa", label: "$" },
//   { value: "ruble", label: "R" },
//   { value: "euro", label: "E" },
// ];
// state = {
//   selectedOption: null,
// };
// handleChange = (selectedOption) => {
//   this.setState({ selectedOption }, () =>
//     console.log(`Option selected:`, this.state.selectedOption)
//   );
// };
// customStyles = {
//   control: (base) => ({
//     ...base,
//     width: 60,

//     borderColor: "red",
//     borderStyle: "none",
//     borderWidth: 0,
//     ":hover": {
//       border: "none",
//     },
//   }),
//   // ,
//   // console.log(base)

//   option: (base) => ({
//     ...base,
//     border: "none",
//     textAlign: "center",
//     ":hover": {
//       backgroundColor: "#818181",
//     },
//   }),

//   dropdownIndicator: (base) => ({
//     ...base,

//     // вокруг стрелки   display: "none",   background: "green",
//   }),
//   indicatorsContainer: (base) => ({
//     ...base,
//     width: 30,
//   }),
//   indicatorSeparator: (base) => ({
//     ...base,
//     marginBottom: 0,
//     marginTop: 0,
//     backgroundColor: "#303030",
//   }),
// };
// import React from "react";

// export default class SelectCurrency extends React.Component {
//   constructor() {
//     super();
//     this.state = { value: "coconut" };

//     this.handleChange = this.handleChange.bind(this);
//   }

//   handleChange(event) {
//     this.setState({ value: event.target.value });
//   }

//   render() {
//     console.log(this.state.value);
//     return (
//       <select value={this.state.value} onChange={this.handleChange}>
//         <option className="option" value="grapefruit">
//           Грейпфрут
//         </option>
//         <option value="lime">Лайм</option>
//         <option value="coconut">Кокос</option>
//         <option value="mango">Манго</option>
//       </select>
//     );
//   }
// }
